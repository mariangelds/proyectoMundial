<h2>Ingresar</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), :html => {:class => 'form-horizontal'}) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, label: 'Correo Electrónico', placeholder: 'usuario@dominio.com', required: false, autofocus: true %>
    <%= f.input :password, label: 'Contraseña', required: false %>
    <%= f.input :remember_me, label: 'Recordarme', as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Ingresar" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
